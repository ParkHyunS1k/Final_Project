# 이 파일을 .env 로 복사하고 값을 채운다. .env 는 커밋되지 않는다(.gitignore).
#   copy .env.example .env
GEMINI_API_KEY=

# 선택. 비워두면 gemini-2.5-flash.
# 주의: 무료 티어 한도가 모델마다 다르다. gemini-2.5-flash는 20회/일이라 파일럿 한 바퀴에 소진된다.
# gemini-2.5-flash-lite는 지원 종료(404). 확인된 대안: gemini-3.1-flash-lite
GEMINI_MODEL=
