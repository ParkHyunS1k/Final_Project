# 사업주 안전조치 의무 이행 검증 시스템

영상 기반 산업안전보건법 위반 판정 및 법령 근거 리포팅 · 온프레미스 구현
부트캠프 파이널 프로젝트 · 13주

---

## 0. 이 문서의 역할

프로젝트의 모든 결정 사항이 여기 있다. 작업 전 반드시 읽고, 여기 명시된 제약을 어기지 말 것. 판단이 필요한 상황에서 이 문서와 충돌하는 방향이 보이면 진행하지 말고 먼저 물어볼 것.

---

## 1. 프로젝트 정의

### 한 줄 요약

**영상으로 산업안전보건법상 사업주의 안전조치 의무 이행 여부를 검증하고, 미이행 시 조항 근거와 법적 리스크를 명시한 리포트를 자동 생성한다.**

### 왜 "사업주 의무"인가

산업안전보건기준에 관한 규칙은 조문이 거의 예외 없이 다음 형태다.

> 사업주는 ~하여야 한다
> 사업주는 근로자가 ~하지 않도록 할 것

행동을 규정한 제67조, 제68조조차 의무 주체가 사업주다. 제재도 사업주는 형사처벌(5년 이하 징역 또는 5천만원 이하 벌금), 근로자는 과태료 5만원(1차)으로 비중이 다르다.

**따라서 이 시스템의 출력은 개인 지목이 아니라 "이 구역에서 해당 의무가 이행되지 않고 있다"이다.** 설비 미설치와 근로자 위험 행동이 모두 사업주 의무 미이행의 증거로 수렴한다.

| 관측 | 의무 미이행의 종류 |
|---|---|
| 안전난간이 없다 | 방호 조치를 하지 않았다 |
| 작업자가 난간을 딛고 있다 | 그렇게 하지 않도록 관리하지 않았다 |
| 안전대 걸 곳이 없다 | 부착설비를 설치하지 않았다 |
| 안전모를 안 썼다 | 지급하고 착용시키지 않았다 |

### 핵심 가설

**객체의 유무만으로는 법령 위반을 판정할 수 없다.** 같은 탐지 결과라도 작업 맥락이 판정을 가른다.

### 프로젝트의 결론이 될 실험

맥락 의존도가 다른 네 그룹에서 룰 기반 대비 VLM 판정의 개선폭이 얼마나 다른가. 낮은 그룹에서는 차이가 작고 높은 그룹에서는 커야 한다. 이 곡선이 "맥락 판정이 왜 필요한가"를 데이터로 증명한다.

성능 수치 자체가 목표가 아니다. 최신 연구도 유사 과제에서 F1 50퍼센트대다.

---

## 2. 확정된 위반 목록 — 9종

> **이 목록은 고정이다. 추가하지 말 것.**

### A. 보호구 — 지급 및 착용 조치 미이행 (2종)

| 위반 | 507 클래스 | 인스턴스 | 조항 |
|---|---|---|---|
| 안전모 미착용 | UA-04 | 29,622 | 안전보건규칙 제32조 |
| 안전벨트 미착용 | UA-02 | 40,671 | 안전보건규칙 제32조 |

### B. 방호설비 — 설치 조치 미이행 (3종)

| 위반 | 507 클래스 | 인스턴스 | 조항 |
|---|---|---|---|
| 안전난간 미설치 | UC-01, UC-04 | 약 26,500 | 제13조, 제42조, 제43조 |
| 작업발판 미설치 | UC-02 | 7,445 | 제42조, 제56조 |
| 이동식비계 전도방지대 미설치 | UC-05, UC-08 | 약 20,800 | 제68조 |

### C. 관리 미이행 — 행동 (3종) · 실험군

| 위반 | 507 클래스 | 인스턴스 | 조항 |
|---|---|---|---|
| 안전난간을 딛거나 밖으로 몸을 기울임 | UA-06, 12, 24 | 약 27,500 | 제68조 |
| 작업발판 위 2명 이상 탑승 | UA-07, 13, 25 | 약 38,000 | 제68조 (적재하중 250kg) |
| 작업발판 끝단에서 작업 | UA-08, 14, 26 | 약 29,000 | 제67조 (양측 끝부분 작업 금지) |

### D. 부착설비 — 관계 판정 (1종)

| 위반 | 507 클래스 | 인스턴스 | 조항 |
|---|---|---|---|
| 안전대 부착설비 미설치 또는 고리 미체결 | UA-01 | 40,499 | 제44조 (2m 이상) |

### 맥락 의존도 그라데이션 — 실험 1의 x축

```
A 보호구      객체 유무만                    → 룰로 충분할 것
B 방호설비    객체 유무 + 위치 판단          → 룰로 대체로 가능
D 안전대      고리와 앵커포인트 관계         → 룰 한계 시작
C 행동        자세 + 공간 관계 + 시간        → VLM 필요
```

### 제외 — 손대지 말 것

| 제외 | 이유 |
|---|---|
| 사다리 관련 6종 | 인스턴스 4,000 안팎으로 작고 제24조로 조항 체계가 갈린다 |
| 적재물 관련 4종 | 조항 연결이 약하다 |
| 안전난간에 오르는 행위 | 몸을 기울이는 행위와 판정 논리 중복 |
| 적재물을 던지는 행위 | 순간 동작이라 프레임 샘플링으로 놓친다 |
| 출입문 여는 행위 | 조항 연결 불명확 |
| 고임목·과상승방지봉 미설치 | 고소작업차 특화. 제186조로 체계가 다르다 |
| 안전화 미착용 | small object. 사전학습 모델 mAP 0.64로 확인됨 |
| 위험반경(협착), 신호수 배치, 화재·전도 계열 | 도메인이 다르다 |

**W12 이후 신규 항목 추가 금지.**

---

## 3. 법령 체계와 제재 구조

### 조항 위계

```
안전보건규칙 제13조, 제32조, 제42조, 제43조, 제44조, 제56조, 제67조, 제68조
  → 산업안전보건법 제38조 (안전조치)
    → 제168조 (벌칙)
```

### 제재

| 대상 | 근거 | 제재 |
|---|---|---|
| **사업주** | 산안법 제38조 위반 | **5년 이하 징역 또는 5천만원 이하 벌금** |
| 사업주 | 제38조 위반으로 근로자 사망 | 7년 이하 징역 또는 1억원 이하 벌금 |
| **근로자** | 산안법 제40조 위반 | 300만원 이하 과태료 (시행령 별표 35 기준 1차 5만원) |

> **중요: 안전조치 의무 위반은 과태료가 아니라 형사처벌이다.** 리포트에 "과태료"라고 쓰지 말 것. 근로자 과태료가 명확한 것은 보호구 미착용(제40조)뿐이다.

### 주요 조항의 정량 기준

| 조항 | 기준 |
|---|---|
| 제13조 안전난간 | 상부 난간대·중간 난간대·발끝막이판·난간기둥 4요소. 상부 난간대 90cm 이상, 발끝막이판 10cm 이상 |
| 제56조 작업발판 | 폭 40cm 이상, 발판재료 간 틈 3cm 이하, 둘 이상 지지물에 고정 |
| 제67조 말비계 | 기울기 75도 이하, 미끄럼 방지장치, 양측 끝부분 작업 금지 |
| 제68조 이동식비계 | 바퀴 고정 또는 아웃트리거, 최대 적재하중 250kg, 안전난간을 딛거나 받침대·사다리 사용 금지 |
| 제44조 안전대 부착설비 | 높이 2m 이상 장소 |
| 제43조 개구부 | 안전난간·울타리·수직형 추락방망·덮개 중 하나 + 개구부 표시 |

> **단안 영상에서 길이 실측은 불가능하다.** 90cm, 40cm, 3cm 같은 정량 기준의 직접 검증은 범위 밖으로 두고 **구성 요소의 존재 여부만 판정**한다. 이 한계를 문서에 명시할 것.

---

## 4. 리포트 출력 형식

```
감지 시각    2026-09-14 14:23:07
구역        3동 5층 서측 단부
관측 사실    작업발판 단부, 안전난간 구성요소 미확인
위반 조항    안전보건규칙 제13조, 제43조
상위 근거    산업안전보건법 제38조(안전조치)
사업주 리스크 5년 이하 징역 또는 5천만원 이하 벌금
            (근로자 사망 시 7년 이하 징역 또는 1억원 이하 벌금)
권고 조치    안전난간 설치 또는 추락방호망 설치 (제42조 단계적 조치)
```

**"관측 사실"과 "위반 조항"을 반드시 분리한다.** 모델은 본 것만 말하고, 조항 연결은 RAG가 하고, 최종 판단은 사람이 한다.

**개인 식별 정보를 저장하지 않는다.** 얼굴은 블러 처리하고 알람은 개인이 아닌 구역 단위로 발생시킨다.

---

## 5. 아키텍처

```
영상 입력 (프레임 샘플링)
  → [1] 장면 인식
  → [2] 객체 탐지 (YOLO)
  → [3] 공간 관계 계산 (거리, 수직관계, 자세)
  ├─ 1차 계층 (실시간 30fps): 룰 엔진 → 즉시 알람
  └─ 2차 계층 (비동기 1-3초)
       → [4] 구조화 프롬프트 생성
       → [5] 로컬 sVLM 맥락 판정
       → [6] 법령 retriever 그라운딩
       → [7] 조항 실재 검증 (국가법령정보 API 재조회)
       → 근거 리포트
```

즉시 경고가 필요한 것과 근거가 필요한 것의 시간 요구가 다르다. 로컬 sVLM은 이미지당 1-3초라 실시간 판정이 불가능하다. 비용 측면에서도 탐지가 트리거한 이벤트에만 VLM을 실행해야 한다.

---

## 6. 데이터

### 주 학습 데이터 — AI Hub 고소작업 현장 실시간 영상 (dataSetSn=507)

- 506.60 GB, 2021 구축
- 어노테이션: Box + Keypoint, 이미지당 객체 1.9개
- 촬영 시점: CCTV, 안전모 부착 카메라(LTE), 액션캠
- 클래스 체계: WO(객체) / SO(설비) / UA(불안전 행동) / UC(불안전 상태)

https://aihub.or.kr/aihubdata/data/view.do?dataSetSn=507

**WO 클래스와 UA 클래스는 동일 인스턴스다.** WO-04 안전모 미착용 29,600과 UA-04 29,622는 같은 객체를 두 체계로 라벨링한 것이다. 데이터가 두 배라고 착각하지 말 것.

### 3단계 데이터 — AI Hub 건설 현장 위험 상태 판단 (dataSetSn=71407)

추락 계열 시나리오 3쌍만 사용. 정상/비정상이 이미 라벨링돼 있다.

- Y-03 / N-03 개구부 덮개 설치 및 불량 (4,000장)
- Y-04 / N-04 개구부 안전난간 설치 및 불량 (4,000장)
- Y-05 / N-05 단부 안전난간 설치 및 불량 (4,000장)

B의 안전난간과 같은 대상이므로 **연출 데이터 두 종류 간 일반화**를 잰다.

https://aihub.or.kr/aihubdata/data/view.do?dataSetSn=71407

### 외부 검증셋 — 학습에 절대 사용 금지

AI Hub 데이터는 시나리오를 연출해 촬영한 것이다. 시나리오당 정확히 2,000장씩 떨어지는 구조가 이를 보여준다. 연출 데이터로 학습하고 연출 데이터로 평가하면 성능이 부풀려진다.

| 데이터 | 검증 대상 | 확보 |
|---|---|---|
| SHWD | 안전모 미착용 | https://github.com/njvisionpower/Safety-Helmet-Wearing-Dataset |
| Construction-PPE | 보호구 전반 | https://docs.ultralytics.com/datasets/detect/construction-ppe |
| SODA | ~~안전난간, 비계~~ **매핑 불가**. `hook`은 타워크레인 후크, `fence`는 가설 울타리, `scaffold`는 비계다. 실제 현장 사진이라 정성 확인용으로만 쓴다 (`docs/data_plan.md` 8절) | https://arxiv.org/abs/2202.09554 |

클래스 하나만 매핑하면 되므로 각 하루면 충분하다. 학습에 섞으면 클래스 통합에만 1-2주가 소요되므로 하지 않는다.

### 법령 데이터

국가법령정보 공동활용 Open API로 산업안전보건기준에 관한 규칙과 산업안전보건법 전문을 확보하고 조·항·호 구조로 파싱한다. **동일 API를 모델이 인용한 조항의 실재 여부 검증에도 사용한다.**

https://open.law.go.kr/

### 사전학습 모델 — W1 zero-shot 베이스라인용

| 모델 | 클래스 | 라이선스 |
|---|---|---|
| melihuzunoglu/ppe-detection | Human, Helmet, No-Helmet, Vest (YOLOv11) | AGPL-3.0 |
| Tanishjain9/yolov8n-ppe-detection-6classes | helmet 0.90, Vest 0.90, safety_shoe 0.64 (mAP@50) | MIT |

---

## 7. 기술 스택

| 구성 | 선택 | 비고 |
|---|---|---|
| 객체 탐지 | Ultralytics YOLO | 1단계는 파인튜닝, 2단계는 좌표 제공용 |
| 자세 추정 | YOLOv8-pose 또는 RTMPose | 사전학습 모델 사용. 507 Keypoint는 검증용 |
| 맥락 판정 VLM | VARCO-VISION 2.0 1.7B | 한국어 특화. 주력 |
| VLM 비교군 | Qwen2.5-VL 3B, Gemma-3 4B, InternVL-3 2B | zero-shot 비교 |
| 파인튜닝 | QLoRA (LLaMA-Factory 또는 ms-swift) | VLM SFT 지원 |
| 법령 retriever | BGE-m3 또는 KURE 계열 | hard negative mining |
| 실험 추적 | W&B 또는 MLflow | seed 3회 반복 기록 |

### 하드웨어

| 구성 | 학습 VRAM | 추론 VRAM |
|---|---|---|
| 1단계 탐지 | 12-16 GB | 8 GB |
| 2단계 VLM 1.7B | 16 GB | 약 2 GB (4bit) |
| 2단계 VLM 4B | 24 GB | 약 4 GB (4bit) |

RunPod Community RTX 4090 기준 시간당 0.34달러. 총 300 GPU시간 예산.

---

## 8. 절대 규칙

### 데이터 분할

> **프레임 단위 랜덤 분할 금지.** 인접 프레임은 거의 동일하므로 train과 test에 나뉘어 들어가면 성능이 크게 부풀려진다. 반드시 **영상 단위 또는 촬영 세션 단위**로 분할한다.

- 비율: train 70 / val 15 / test 15
- 층화 기준: 위반 유형, 촬영 시점(CCTV / 안전모캠 / 액션캠)
- 각 split에 세 촬영 시점이 모두 포함되도록 구성
- 분할 스크립트와 seed를 저장소에 커밋할 것

### 데이터 다운로드

> **AI Hub 데이터를 전량 다운로드하지 말 것.**

샘플(경량) 데이터와 파일 목록 API 다운로드가 제공된다. 9종에 해당하는 클래스만 선택 확보한다. 20-30GB면 충분하다.

### 외부 API

> **운영 아키텍처는 100퍼센트 로컬이다.**

외부 API는 실험 3에서 평가셋 수백 건에 대한 **1회 추론에만** 사용한다. 학습과 운영 파이프라인에는 일절 사용하지 않는다. 호출 경로는 `src/eval/experiments/` 아래로만 격리한다.

### 재현성

- 모든 실험은 **seed 3회 이상 반복**하고 평균과 표준편차로 보고한다
- 단순 baseline을 먼저 만들고 최종 모델의 개선폭을 제시한다

### 스코프

- 게이트 미달 시 정해진 축소안을 따른다. 임의로 일정을 늘리지 않는다
- W12 이후 신규 기능 및 신규 위반 항목 추가 금지

---

## 9. 평가 설계

### 1단계 지표 (A, B 그룹)

- mAP@0.5, mAP@0.5:0.95, **클래스별 AP 필수 보고**
- 사람 단위 precision / recall — 안전 도메인이므로 **recall 우선**
- PR 곡선으로 임계값 선택하고 근거 기술
- 트래킹 스무딩 후 **이벤트 단위 precision / recall**

### 2단계 지표 (C, D 그룹)

- 위반 판정 F1 — **난이도 3계층별, 그룹별로 분리 보고**
- 조항 정확도 — 조·항·호 번호를 정규식으로 자동 채점
- **환각률** — 인용 조항을 법령 API로 재조회해 실재하지 않는 조항 인용 비율
- 설명 품질 — BERTScore (선행 연구와 동일 지표)
- 1차 알람 지연시간

> 조·항·호는 정규식으로 정확히 채점 가능하다. **LLM-as-judge에 의존하지 않는 설계**이며 이 점을 발표에서 강조한다.

### 핵심 실험 3종

| 번호 | 실험 | 외부 API | 내용 |
|---|---|---|---|
| 실험 1 | 룰 기반 vs VLM 맥락 판정 | 로컬 | **A → B → D → C 순으로 개선폭이 커지는가.** 프로젝트의 결론 |
| 실험 2 | 로컬 모델 규모별 비교 | 로컬 | 1.7B / 3B / 4B / (가능시) 14B zero-shot 스케일링 |
| 실험 3 | 로컬 vs 외부 API | API 1회 | 정확도, 지연시간, 비용. 배포는 로컬 전용임을 명시 |

### 합격 기준

| 항목 | 최소 | 목표 |
|---|---|---|
| 1단계 탐지 mAP@0.5 | 0.70 | 0.85 |
| 2단계 판정 F1 (명백 계층) | 0.60 | 0.75 |
| 2단계 판정 F1 (전체) | 0.45 | 0.55 |
| 조항 정확도 | 0.80 | 0.90 |
| 환각률 | 10퍼센트 이하 | 3퍼센트 이하 |
| 라벨러 kappa | 0.60 | 0.75 |
| 1차 알람 지연 | 1초 이하 | 200ms 이하 |

### 맥락 판정 벤치마크 (팀 자체 구축)

규모 200-300장. 9종 대상에 한정.

라벨 항목: 작업 유형 / 위반 유형 / 실제 위반 여부 / 적용 조항 / 위험 등급 / 난이도(명백·경계·모호)

**필수 조건** — 같은 객체 상태인데 맥락에 따라 판정이 갈리는 케이스를 의도적으로 포함할 것.

품질 관리: 라벨링 가이드라인 문서화 → 2인 독립 라벨링 → 불일치 조정 → **Cohen's kappa 측정 및 보고**

---

## 10. 13주 일정과 게이트

| 주차 | 작업 |
|---|---|
| W1 | AI Hub 507 신청 및 샘플 확인, 법령 API 키 발급, 로컬 VLM 구동 테스트, 사전학습 모델 zero-shot 베이스라인 |
| W2 | 산안법 및 안전보건규칙 조문 파싱, **9종 조항 매핑 테이블 확정** |
| W3 | 507 전처리, 영상 단위 분할 설계, A그룹 2종 탐지 베이스라인 |
| W4 | B그룹 3종 추가, 탐지 모델 파인튜닝 |
| W5 | 룰 엔진 구현, 법령 매칭 연결 |
| W6 | 리포트 자동생성, 엔드투엔드 통합, **A·B 룰 베이스라인 성능 확정** |
| W7 | Keypoint 파이프라인 연결, 구조화 프롬프트 설계, VLM zero-shot 측정 |
| W8 | C·D그룹 4종 라벨 정제 및 학습셋 구성 |
| W9 | 로컬 sVLM QLoRA 파인튜닝, JSON 구조화 출력 안정화 |
| W10 | 법령 retriever 파인튜닝, 환각률 측정, **실험 1**, 외부 검증셋 평가 |
| W11 | **실험 2**, **실험 3**, seed 3회 반복 |
| W12 | 3단계 71407 교차 검증, 2단 알람 통합, 데모 UI |
| W13 | 실패 유형 분석, 문서화, 발표 준비 |

### 게이트

| 시점 | 기준 | 미달 시 |
|---|---|---|
| W3 종료 | 507 확보, A그룹 2종 탐지 동작 | 일정 전면 재검토 |
| W6 종료 | A·B 5종 엔드투엔드 + 룰 베이스라인 확보 | B그룹 1종을 2단계로 이월 |
| W10 종료 | C·D 4종 판정 + 실험 1 완료 | C그룹 1종만 남기고 심화 |
| W12 종료 | 3단계까지 완료 | 3단계 포기, 1·2단계 심화로 마무리 |

### 최소 성공 기준

**A·B 5종 + C그룹 1종.** 대조군과 실험군의 룰 대비 개선폭 비교 수치 하나면 프로젝트의 논지가 증명된다. 위반 유형의 개수는 프로젝트의 가치가 아니다.

---

## 11. 디렉터리 구조

```
.
├── CLAUDE.md
├── README.md
├── pyproject.toml
├── configs/
│   ├── data/           # 분할 설정, 클래스 매핑
│   ├── detect/         # YOLO 학습 설정
│   ├── vlm/            # QLoRA 설정, 프롬프트 템플릿
│   └── law/            # 9종 조항 매핑 테이블
├── src/
│   ├── data/
│   │   ├── aihub.py        # 507, 71407 파서
│   │   ├── split.py        # 영상 단위 분할 (seed 고정)
│   │   └── external.py     # SHWD, Construction-PPE, SODA 로더
│   ├── detect/
│   │   ├── train.py
│   │   ├── predict.py
│   │   └── tracking.py     # 스무딩, 이벤트 단위 변환
│   ├── pose/
│   │   └── estimate.py     # 사전학습 pose 모델 래퍼
│   ├── spatial/
│   │   └── relations.py    # 거리, 수직관계, 자세 특징
│   ├── rules/
│   │   └── engine.py       # 1차 계층 룰 판정 (베이스라인)
│   ├── vlm/
│   │   ├── prompt.py
│   │   ├── infer.py
│   │   └── finetune.py     # QLoRA
│   ├── law/
│   │   ├── fetch.py        # 국가법령정보 API
│   │   ├── parse.py        # 조·항·호 파싱
│   │   ├── retriever.py
│   │   └── verify.py       # 환각 검증 (조항 재조회)
│   ├── report/
│   │   └── generate.py
│   └── eval/
│       ├── metrics.py
│       ├── benchmark.py
│       └── experiments/    # 실험 1, 2, 3
├── scripts/
├── notebooks/
├── data/                  # gitignore
├── outputs/               # gitignore
└── docs/
    ├── labeling_guide.md
    └── law_mapping.md     # 9종 위반 - 조항 매핑
```

---

## 12. 코드 규칙

- Python 3.11 이상. 의존성은 pyproject.toml로 관리
- 모든 설정은 하드코딩하지 말고 configs 아래 YAML로 분리
- 실험 스크립트는 seed를 인자로 받고 결과에 seed를 기록
- 외부 API 호출은 `src/eval/experiments/` 아래로만 격리
- 데이터 경로는 환경변수 또는 config로. 절대경로 하드코딩 금지

---

## 13. 자주 하는 실수 — 하지 말 것

| 하지 말 것 | 이유 |
|---|---|
| 프레임 단위 랜덤 분할 | 성능이 크게 부풀려진다 |
| AI Hub 데이터 전량 다운로드 | 스토리지 비용과 시간 낭비 |
| WO 클래스와 UA 클래스를 별개 데이터로 취급 | 동일 인스턴스다 |
| 리포트에 "과태료" 표기 | 안전조치 위반은 형사처벌이다. 근로자 과태료는 보호구 미착용뿐 |
| 개인을 지목하는 출력 | 의무 주체는 사업주다. 구역 단위로 출력한다 |
| 정량 기준(90cm 등) 실측 시도 | 단안 영상에서 불가능. 구성 요소 존재 여부만 판정 |
| 2단계에서 탐지 성능 튜닝 | 탐지는 좌표 제공용이다. mAP 경쟁 금지 |
| 외부 검증셋을 학습에 섞기 | 클래스 통합에 1-2주가 날아간다 |
| 단일 seed 결과 보고 | 신뢰받지 못한다 |
| 위반 항목 추가 | 9종 고정이다 |
| 모든 프레임에 VLM 실행 | 이미지당 1-3초. 이벤트 트리거 방식이어야 한다 |
| F1 50퍼센트대를 실패로 간주 | 최신 연구도 그 수준이다 |

---

## 14. 착수 전 확인 필요

이 두 가지는 507 데이터 명세서를 받아야 확정된다.

1. **UC-01(17,185)과 UC-04(9,309)가 둘 다 "안전난간 미설치"다.** 공정이 다를 것으로 추정(비계 대 고소작업차). 병합할지 분리할지 결정 필요. UC-05와 UC-08(전도방지대)도 동일.
2. **제44조는 높이 2m 이상이 전제다.** 단안 영상에서 높이 판정이 어려우므로 고소작업 데이터셋 특성상 대부분 해당한다고 가정하고, 높이 검증은 범위 밖으로 명시한다.

---

## 15. 지금 할 일

1. 저장소 초기 구조 생성 (섹션 11 참조), pyproject.toml, .gitignore
2. `src/law/fetch.py`와 `parse.py` 구현 — 국가법령정보 API로 산업안전보건법과 안전보건규칙을 받아 조·항·호 구조로 파싱. **API 키가 없으면 알려줄 것**
3. `docs/law_mapping.md` — 섹션 2의 9종에 대응하는 조·항·호를 원문에서 확인해 표로 정리. 섹션 3의 제재 구조도 함께 기술
4. 사전학습 PPE 모델 2종을 받아 zero-shot 추론 스크립트 작성 (`scripts/baseline_zeroshot.py`)

AI Hub 데이터 승인을 기다리는 동안 2, 3, 4를 진행한다. 데이터가 없어도 되는 작업들이다.

각 단계를 시작하기 전에 무엇을 할지 먼저 말하고, 이 문서와 충돌하는 부분이 있으면 진행하지 말고 질문할 것.
