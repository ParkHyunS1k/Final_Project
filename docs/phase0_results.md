# Phase 0 결과 — 라벨 768 MB 실측

507 라벨 전량 + 71407 추락 라벨 전량(합 768 MB, JSON 655,718개)을 받아 분석했다.
원천 데이터 450 GB 없이 라벨만으로 확정한 사항이다.

재현: `scripts/download_aihub.sh phase0` → `python scripts/label_stats.py data/raw/507`

---

## 1. 타겟 클래스가 작업유형에 배타적으로 몰려 있다

507 프레임 619,718개 / 클래스 66종 / 작업유형 6종의 교차표:

| 클래스 | 개구부 | 고소작업대 | 공통 | 로프 | **비계** | 사다리 |
|---|---:|---:|---:|---:|---:|---:|
| `SO-01` 안전난간 | 0 | 0 | 0 | 0 | **58,614** | 0 |
| `SO-02` 안전발판 | 0 | 0 | 0 | 0 | **52,314** | 0 |
| `UC-01` 안전난간 미설치 | 0 | 0 | 0 | 0 | **15,495** | 0 |
| `UC-04` 안전난간 미설치 | 0 | 0 | 0 | 0 | **8,390** | 0 |
| `UA-01` 안전고리 미체결 | 0 | 0 | **36,457** | 0 | 0 | 0 |
| `WO-06` 안전고리 미착용 | 0 | 0 | **36,454** | 0 | 0 | 0 |

**안전난간은 비계작업에만, 안전고리는 공통에만 있다.** 다른 작업유형은 정확히 0건이다.

### 다운로드 계획 변경: 56 GB → 38 GB

| 대상 | filekey | 용량 | 근거 |
|---|---|---|---|
| VS_03 공통 | 68034 | 15 GB | 안전고리 |
| VS_05 비계작업 | 68036 | 23 GB | 안전난간 |

개구부(488 MB)·로프(428 MB)·사다리(3 GB)·고소작업대(14 GB)는 타겟 클래스가 0건이므로 받지 않는다.
로프작업에 안전고리가 있을 것으로 예상했으나 `WO-01`과 `SO-24`뿐이었다.

---

## 2. README 13절 "WO와 UA는 동일 인스턴스다"는 사실과 다르다

`WO-06`(안전고리 미착용)과 `UA-01`(안전고리 미체결)의 관계를 실측했다.

| 항목 | 값 |
|---|---|
| 두 코드가 함께 등장하는 프레임 | 36,452 |
| IoU > 0.5 로 매칭되는 박스 | 1,151 / 36,454 = **3.2%** |
| `UA-01` 면적 중앙값 | 0.0323 (`WO-01` 작업자의 **0.76배**) |
| `WO-06` 면적 중앙값 | 0.0114 (`WO-01`의 **0.27배**) |

개수만 40,496 대 40,499로 비슷할 뿐, **서로 다른 대상을 라벨링한 별개의 어노테이션**이다.

- `UA-01` — 작업자 전신에 가까운 크기. 사람 단위 **상태** 라벨
- `WO-06` — 그보다 훨씬 작음. 고리·부착부 **부품** 라벨

> README 13절의 "데이터가 두 배라고 착각하지 말 것"은 근거가 성립하지 않는다.
> 실제로는 성격이 다른 두 라벨이 모두 존재하며, 둘 다 쓸 수 있다.
> 안전모(`WO-04` 0.0020 vs `UA-04` 0.0386)도 20배 차이로 같은 패턴이다.

### 결론: A안 채택

`docs/data_plan.md` 2절에서 제기한 "안전고리에 positive 클래스가 없다" 리스크는 해소됐다.
`WO-06`을 부품 클래스로 학습하고, `UA-01`은 사람 단위 판정의 보조 라벨로 쓴다.

---

## 3. imgsz 640 으로 충분하다

`WO-06`이 작은 객체라 960이 필요할지 우려했으나, 실측 결과 불필요하다.

| | 면적비 | 원본(1920×1080) | imgsz 640 |
|---|---|---|---|
| `WO-06` 중앙값 | 0.0114 | 154 × 154 px | 51 × 51 px |
| `WO-06` p10 | 0.0077 | 126 × 126 px | 42 × 42 px |
| `SO-01` 중앙값 | 0.0580 | 347 × 347 px | 116 × 116 px |

640에서도 40 px 이상을 유지한다. **학습 시간이 28시간에서 13시간으로 줄었다.**

참고로 진짜 작은 객체는 `WO-04` 안전모 미착용(0.0020 → 640에서 21×21)과
`WO-05` 안전벨트 미착용(0.0064 → 640에서 37×37)이다. 9종으로 확장할 때 재검토할 것.

---

## 4. README 14절 1번 답 — UC-01과 UC-04는 둘 다 비계작업

| | 인스턴스 | 작업유형 | 면적 중앙값 |
|---|---:|---|---|
| `UC-01` | 15,495 | 비계작업 | 0.1121 |
| `UC-04` | 8,390 | 비계작업 | 0.1999 |

공정이 갈린 것이 아니라 **같은 비계작업 안에서 나뉜다.** 같은 프레임에 공존하지 않으며
면적이 1.8배 차이 난다. 촬영 구도나 비계 종류(내부/외부)의 차이로 추정되나
라벨만으로는 확정할 수 없다. **원천 이미지를 받은 뒤 육안 확인이 필요하다.**

`UC-03`(7,348, 0.1949)과 `UC-06`(8,322, 0.2877)도 동일한 패턴이다.

---

## 5. 영상 단위 분할이 성립한다

| 작업유형 | 영상 수 | 어노테이션 |
|---|---:|---:|
| 비계작업 | 564 | 442,584 |
| 고소작업대작업 | 447 | 240,647 |
| 공통 | 172 | 295,121 |
| 사다리작업 | 167 | 63,196 |
| 로프작업 | 17 | 17,116 |
| 개구부작업 | 15 | 12,431 |

우리가 쓸 두 작업유형은 비계 564영상, 공통 172영상이다.
70/15/15 분할 시 공통이 120/26/26 영상으로 나뉜다. 성립하지만 여유가 크지 않다.

---

## 6. 71407 — 시나리오 명칭 확인 완료

README 6절 기재값이 맞았다. 추락 계열 관심 시나리오의 실제 클래스 구성:

| 시나리오 | 주 클래스 | 해석 |
|---|---|---|
| `Y-03` | `SO-07` 1,800 | 개구부 덮개 설치 |
| `N-03` | `SO-19` 1,800 | 개구부 덮개 불량 |
| **`Y-04`** | **`SO-01` 1,800**, `DO-03` 1,660 | **개구부 안전난간 설치** |
| `N-04` | `DO-03` 1,800 | 개구부 안전난간 없음 |
| **`Y-05`** | **`SO-02` 1,800** | **단부 안전난간 설치** |
| `N-05` | `SO-14` 1,800 | 단부 안전난간 없음 |

안전난간 학습에 쓸 수 있는 것은 `Y-04`의 `SO-01`과 `Y-05`의 `SO-02`로 **합 3,600개**다.
507의 58,614개에 비하면 작다.

> **도메인 갭 주의.** 71407 `SO-01`의 면적 중앙값이 **0.4513**으로 화면의 45%를 차지한다.
> 507 `SO-01`의 0.058과 8배 차이다. 71407은 난간을 클로즈업으로 찍었다.
> 학습에 섞으면 스케일 분포가 왜곡되므로 **교차검증 전용으로만 쓴다.**
> 이는 README 6절의 "연출 데이터 두 종류 간 일반화를 잰다"는 목적과도 일치한다.

---

## 6-2. 영상 다양성이 이 데이터셋의 진짜 한계다

타겟이 실제로 라벨된 영상 수를 Training / Validation 파티션별로 세어 봤다.

| 타겟 | 조건 | label_train | label_val |
|---|---|---|---|
| 안전난간 `SO-01` | 비계작업 · WS | 영상 **36** / 프레임 15,418 / 52,150건 | 영상 **36** / 프레임 1,912 / 6,464건 |
| 안전고리 `WO-06` | 공통 · UA | 영상 **15** / 프레임 32,411 / 32,413건 | 영상 **15** / 프레임 4,041 / 4,041건 |

### AI Hub 의 train/val 분할은 영상 단위가 아니다

두 파티션의 **영상 집합이 완전히 동일하다.** 같은 영상에서 프레임만 나눠 담았다.
즉 AI Hub 제공 분할을 그대로 쓰면 train 과 val 에 인접 프레임이 섞여
**성능이 크게 부풀려진다.** README 8절이 경고하는 바로 그 상황이다.

> 이 데이터셋으로 보고된 성능 수치를 인용할 때는 분할 방식을 확인해야 한다.
> 발표에서 짚을 만한 지점이다.

### 결론: Training 원천을 받을 이유가 없다

`TS_05_비계작업` 185 GB, `TS_03_공통` 122 GB 를 받아도 **같은 36개 / 15개 영상에서
프레임이 8배 늘어날 뿐 새로운 장면은 하나도 없다.** 인접 프레임은 거의 동일하므로
유효 데이터 증가량은 미미하다. **307 GB 를 받지 않는다.**

Validation 원천 38 GB 가 최선의 선택임이 확정됐다.

### 대신 감수해야 하는 것

영상 단위 분할 시 남는 영상 수가 적다.

| 타겟 | 영상 | 70/15/15 분할 결과 |
|---|---|---|
| 안전난간 | 36 (+배경 89) | train 87 / val 19 / test 19 영상 |
| 안전고리 | **15** | **train 11 / val 2 / test 2 영상** |

안전고리의 test 가 사실상 장면 2개다. 단일 분할의 지표는 분산이 매우 크다.

권장:
1. 안전고리는 단일 분할 대신 **영상 단위 group k-fold** 로 평가한다 (k=5, 영상 3개씩)
2. README 6절의 외부 검증셋(SODA 등) 평가가 선택이 아니라 **필수**가 된다.
   15개 영상에서 학습한 모델의 일반화를 내부 지표로 주장할 수 없다
3. seed 3회 반복(README 8절)에 **분할 seed 변경도 포함**한다. 학습 seed만 바꾸면
   영상 배정이 고정되어 진짜 분산이 드러나지 않는다

---

## 7. 다음 단계에 반영할 리스크

### 두 타겟이 서로 다른 작업유형에 배타적으로 존재한다

안전난간은 비계작업 이미지에만, 안전고리는 공통 이미지에만 라벨이 있다.
**두 파티션을 합쳐 단일 모델로 학습하면 서로의 클래스에 대해 라벨 없는 positive가 생긴다.**
공통 이미지에 안전난간이 찍혀 있어도 라벨이 없으므로 모델은 그것을 배경으로 학습한다.

권장: **안전난간 모델과 안전고리 모델을 분리 학습한다.**
합칠 경우 클래스별 loss 마스킹이 필요하며 Ultralytics 기본 학습 루프로는 지원되지 않는다.

### 확인 필요

- `UC-01` / `UC-04` 의 실제 차이 (원천 이미지 육안 확인)
- `WO-06` 박스가 고리 자체인지 부착부 전체인지 (원천 이미지 육안 확인)
